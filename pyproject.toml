[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "local-agent-runtime"
version = "0.1.0"
description = "A small local-first runtime for skills, tools, authorization and auditable agent workflows."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "AUREVAN Engineering"}]
dependencies = []
